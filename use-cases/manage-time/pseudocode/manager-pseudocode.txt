
getRequests(managerId,paginationDto)
{
    return getRequests(managerId,paginationDto);
}

showRequest(requestId)
{
     validateRequestExist(requestId);
     return getRequest(requestId);
}

approveRequest(requestId)
{
    validateRequestExist();
    validateRequestStatus();
    updateRequest();
    sendEmployeeEmail(requestId);
}

rejectRequest(requestId,rejectRequestDto)
{
    validateRequestExist();
    validateRequestStatus();
    updateRequest(rejectRequestDto);
    sendEmployeeEmail(requestId);
}





